from __future__ import annotations

# Signal receivers are registered in later changes (periodic import task and the
# organizer-level navigation entry). Kept as an importable module so that
# ``PluginApp.ready()`` has something to load.
